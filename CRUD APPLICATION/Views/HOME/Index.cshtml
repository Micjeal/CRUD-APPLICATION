@model IEnumerable<CRUD_APPLICATION.Models.Employeetbl>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ENAME)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ELOCATION)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ESALARY)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ENAME)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ELOCATION)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ESALARY)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EID }) |
            @Html.ActionLink("Details", "Details", new { id=item.EID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EID })
        </td>
    </tr>
}

</table>
